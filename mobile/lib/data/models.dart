// All data classes (and enums) are generated from the Zod schemas in
// @patiently/shared — keeping the mobile contract in lock-step with the server.
// Run `npm run codegen` from the repo root to regenerate `api.g.dart`.
export 'api.g.dart';
