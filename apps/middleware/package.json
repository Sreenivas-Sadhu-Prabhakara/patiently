{
  "name": "@patiently/middleware",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API gateway / backend-for-frontend: auth, validation, routing and scheduling. The single entry point for web and mobile clients.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/jwt": "^9.0.1",
    "@patiently/backend": "*",
    "@patiently/shared": "*",
    "fastify": "^5.2.0",
    "zod": "^3.24.1"
  }
}
